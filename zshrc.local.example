# Copy this to ~/.zshrc.local and fill in machine/work-specific settings.
# ~/.zshrc.local is sourced at the end of zshrc and is NOT tracked in this repo.

# Google Cloud SDK (path varies per install location)
# if [ -f "$HOME/google-cloud-sdk/path.zsh.inc" ]; then . "$HOME/google-cloud-sdk/path.zsh.inc"; fi
# if [ -f "$HOME/google-cloud-sdk/completion.zsh.inc" ]; then . "$HOME/google-cloud-sdk/completion.zsh.inc"; fi

# Work-specific paths / env
# export KUBE_DIR="$HOME/path/to/kube/repo"

# Work-specific functions / aliases go here.
